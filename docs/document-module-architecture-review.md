# Отчет о проверке архитектуры модуля Document

**Дата проверки:** 2026-02-18  
**Модуль:** `com.example.interhubdev.document`

## ✅ Общая оценка

Архитектура модуля **соответствует** правилам модульной архитектуры проекта. Связи с другими модулями ясные и архитектурно оправданные.

---

## 1. Структура модуля

### ✅ package-info.java
- Корректно объявлен `@ApplicationModule` с `displayName = "Document"`
- `allowedDependencies = {"error", "auth", "user"}` — правильно указаны зависимости
- Подробная документация публичного API и зависимостей

### ✅ Публичный API
Все публичные типы находятся в корне пакета `document`:
- **API интерфейсы:** `DocumentApi`, `CourseMaterialApi`, `HomeworkApi`
- **DTO:** `StoredFileDto`, `CourseMaterialDto`, `HomeworkDto`
- **Порты:** `LessonLookupPort`, `OfferingLookupPort`, `StoragePort`, `UploadSecurityPort`
- **Вспомогательные типы:** `UploadContext`, `UploadResult`, `UploadPurpose` (enum)

### ✅ Внутренняя структура
Все детали реализации находятся в пакете `internal`:
- Контроллеры: `DocumentController`, `CourseMaterialController`, `HomeworkController`, `CourseMaterialByIdController`, `HomeworkByIdController`
- Сервисы: `DocumentServiceImpl`, `CourseMaterialServiceImpl`, `HomeworkServiceImpl`
- Репозитории: `StoredFileRepository`, `CourseMaterialRepository`, `HomeworkRepository`, `LessonHomeworkRepository`
- Сущности: `StoredFile`, `CourseMaterial`, `Homework`, `LessonHomework`
- Ошибки: `DocumentErrors`, `CourseMaterialErrors`, `HomeworkErrors`
- Мапперы: `StoredFileMappers`, `CourseMaterialMappers`, `HomeworkMappers`

---

## 2. Зависимости между модулями

### ✅ Прямые зависимости (allowedDependencies)
Модуль корректно зависит только от разрешенных модулей:
- **error** — используется для всех бизнес-ошибок через `Errors` и модульные `*Errors`
- **auth** — используется `AuthApi.getCurrentUser()` для получения текущего пользователя
- **user** — используется `UserApi` для проверки ролей и прав доступа

### ✅ Обратные зависимости через порты и адаптеры

#### LessonLookupPort
- **Порт объявлен:** `com.example.interhubdev.document.LessonLookupPort`
- **Использование:** В `HomeworkServiceImpl` для валидации существования урока
- **Адаптер:** `com.example.interhubdev.adapter.LessonLookupAdapter`
  - Реализует порт через `ScheduleApi.findLessonById()`
  - Находится в пакете `adapter`, который зависит от `schedule` и `document`
  - ✅ Архитектурно правильно: нет прямой зависимости document → schedule

#### OfferingLookupPort
- **Порт объявлен:** `com.example.interhubdev.document.OfferingLookupPort`
- **Использование:** В `CourseMaterialServiceImpl` для валидации существования offering
- **Адаптер:** `com.example.interhubdev.adapter.OfferingLookupAdapterForDocument`
  - Реализует порт через `OfferingExistsPort.existsById()`
  - Находится в пакете `adapter`, который зависит от `offering` и `document`
  - ✅ Архитектурно правильно: нет прямой зависимости document → offering

### ✅ Проверка отсутствия запрещенных зависимостей
Проверено: **нет импортов** из модулей `schedule`, `offering`, `group`, `program`, `curriculum` в коде модуля `document`.

---

## 3. Обработка ошибок

### ✅ Единая точка обработки
Все ошибки идут через модуль `error`:
- Используются фабрики `Errors.badRequest()`, `Errors.notFound()`, `Errors.forbidden()`, и т.д.
- Модульные классы ошибок (`DocumentErrors`, `CourseMaterialErrors`, `HomeworkErrors`) возвращают `AppException`
- Все исключения обрабатываются глобальным `GlobalExceptionHandler`

### ✅ Коды ошибок
Модульные классы ошибок содержат константы кодов для фронтенда:
- `CODE_STORED_FILE_NOT_FOUND`, `CODE_FILE_TOO_LARGE`, и т.д.
- Сообщения пригодны для отображения пользователю

---

## 4. Контроллеры

### ✅ Тонкий слой
Все контроллеры соответствуют принципу "тонкого слоя":
- Только парсинг запроса (path, query, body)
- Вызов методов `*Api`
- Преобразование в `ResponseEntity`
- Минимум бизнес-логики

### ✅ Использование API
Контроллеры зависят только от:
- `*Api` интерфейсов (DocumentApi, CourseMaterialApi, HomeworkApi)
- `AuthApi` для получения текущего пользователя
- Нет прямых зависимостей от сервисов, репозиториев или сущностей

### ⚠️ Небольшое замечание
В контроллерах `HomeworkByIdController` и `CourseMaterialByIdController` есть прямое использование `*Errors` для бросания исключений при `Optional.empty()`:
```java
.orElseThrow(() -> HomeworkErrors.homeworkNotFound(homeworkId))
```
Это допустимо, так как ошибки все равно идут через модуль `error`, но идеально было бы, чтобы API сам бросал исключения. Однако текущая реализация не нарушает архитектуру.

---

## 5. Сервисы

### ✅ Реализация API
Все сервисы корректно реализуют публичные API интерфейсы:
- `DocumentServiceImpl implements DocumentApi`
- `CourseMaterialServiceImpl implements CourseMaterialApi`
- `HomeworkServiceImpl implements HomeworkApi`

### ✅ Использование портов
Сервисы используют порты для обратных зависимостей:
- `CourseMaterialServiceImpl` инжектит `OfferingLookupPort`
- `HomeworkServiceImpl` инжектит `LessonLookupPort`
- Порты реализуются адаптерами из пакета `adapter`

### ✅ Транзакции
Транзакции проставлены корректно:
- Read-методы: `@Transactional(readOnly = true)`
- Write-методы: `@Transactional`

### ✅ Бизнес-логика
Валидация и бизнес-правила находятся в сервисах:
- Проверка прав доступа
- Валидация данных
- Бизнес-правила (например, проверка использования файла перед удалением)

---

## 6. Маппинг Entity → DTO

### ✅ Инкапсуляция
Маппинг находится в `internal`:
- `StoredFileMappers`, `CourseMaterialMappers`, `HomeworkMappers`
- Сущности не утекают за пределы модуля
- Публичные DTO не знают о сущностях JPA

---

## 7. Документация

### ✅ Javadoc
- Публичные API интерфейсы имеют полную документацию с `@param`, `@return`, `@throws`
- Классы ошибок документированы
- Порты имеют описание назначения
- `package-info.java` содержит подробное описание модуля

---

## 8. Безопасность и эффективность

### ✅ Проверка прав доступа
- Все операции проверяют права доступа
- Используются роли из модуля `user`
- Разделение прав: owner, TEACHER, ADMIN, MODERATOR, SUPER_ADMIN

### ✅ Защита от удаления используемых файлов
- `DocumentServiceImpl.checkFileNotInUse()` проверяет использование файла перед удалением
- Проверяется использование в `CourseMaterial` и `Homework`

### ✅ Атомарность операций
- Загрузка файла атомарна: при ошибке БД файл удаляется из S3

---

## 9. Рекомендации

### ✅ Все хорошо
Архитектура модуля соответствует всем требованиям. Связи с другими модулями ясные и архитектурно оправданные через порты и адаптеры.

### 💡 Необязательные улучшения (не критично)
1. **Контроллеры:** Рассмотреть возможность, чтобы API сам бросал исключения при `Optional.empty()`, а не контроллер. Но текущая реализация допустима.
2. **Документация:** Можно добавить больше примеров использования в Javadoc, но текущая документация достаточна.

---

## Заключение

**Модуль `document` архитектурно корректен.** Все связи с другими модулями реализованы через порты и адаптеры, что исключает циклические зависимости и обеспечивает четкое разделение ответственности. Структура модуля соответствует правилам проекта, все зависимости разрешены, ошибки обрабатываются единообразно через модуль `error`.

**Оценка:** ✅ **Отлично**
